<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin</title>
    <link rel="stylesheet" href="styleAdmin.css">
</head>
<body>
    <nav>
        <div class="navlinks">
            <a href="/home">Heim</a>
            <a href="/logout">Logg ut</a>
            <% if (admin) { %>
                <a href="/admin">Admin </a>
                <% } %>
        </div>
    <h2>Admin site</h2>
    <h3>Users</h3>
    <table>
        <tr>
            <th>Fornavn</th>
            <th>Etternamn</th>
            <th>Email</th>
            <th>Rediger</th>
            <th>Slett</th>
        </tr>
        <% users.forEach((user) => { %>
        <tr>
            <td><%= user.firstname %> </td>
            <td><%= user.lastname %> </td>
            <td><%= user.email %> </td>
            <td><a href="/admin/edit/<%=user.email%>">Edit</a></td>
            <td>
                <!-- Change the form action to use user's email instead of ID -->
                <form action="/admin/delete/<%= user.email %>" method="post" onsubmit="return confirm('Er du sikker på at du vil slette denne brukeren?');">
                    <button type="submit">Slett</button>
                </form>
            </td>
        </tr>
        <% }) %>
    </table>
</body>
</html>